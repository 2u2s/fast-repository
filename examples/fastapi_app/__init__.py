"""A FastAPI app showing fast-repository wired in through dependency injection.

Split by responsibility — ``database`` (infrastructure), ``dependencies`` (DI),
``dtos`` (schemas), ``routers`` (endpoints), ``main`` (assembly).
"""
